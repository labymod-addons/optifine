.java_docker :
  image : amazoncorretto:17.0.0-alpine
  tags :
    - docker

build :
  stage : build
  extends : .java_docker
  script :
    - sh gradlew build --full-stacktrace
    - cp core/build/libs/optifine-* optifine.jar
  artifacts:
    paths:
      - optifine.jar